services:
  Tweetcord:
    build: .
    image: ghcr.io/fusefairy/tweetcord-qq:0.1.1
    container_name: tweetcord
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data/:/bot/data
      - ./configs.yml:/bot/configs.yml